<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cby.contentprovidertest1">

    <permission android:name="com.cby.PROVIDER" android:protectionLevel="normal"/>
    <permission android:name="com.cby.READ" android:protectionLevel="normal"/>
    <permission android:name="com.cby.WRITE" android:protectionLevel="normal"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ContentProviderTest1">
        <provider
            android:name=".MyProvider"
            android:authorities="com.cby.uri.myprovider"
            android:permission="com.cby.PROVIDER"
            android:enabled="true"
            android:exported="true">

        </provider>

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>